import React from 'react';
import './Sport.css'

const Sport = ({sport}) => {
    const {name, img, description, age} = sport;
    return (
        <div>
            
        </div>
    );
};

export default Sport;